# איסוף סניפים לאורך זמן

המאגר הציבורי משותף לכל השוברים. רשימת הרשתות של מוצר השובר קובעת התאמה; הוספת סניף אינה משנה זכאות. האיסוף אינו תהליך שרץ באתר: כל סבב מסתיים בבדיקה ופרסום גרסה חדשה. אין כרגע תזמון מחזורי.

## סבב עבודה

1. בוחרים רשתות דרך הגדרות ← מעקב מיפוי חנויות. מתחילים ברשתות של שוברים פעילים, ללא מיקומים או עם פער גדול. מספר שנותר ידוע רק ביחס לרשימה שנבדקה; מקור חלקי אינו מספר הסניפים הארצי.
2. אפשר להפעיל אוספים במקביל, עם בעלות נפרדת על כל רשת וקובץ `candidates/`. האוספים אינם עורכים את המאגר המשותף או מפרסמים את האתר.
3. שומרים כתובת, קישור מקור, קישור למקור הקואורדינטות ותאריך בדיקה. משתמשים ביעד ניווט מפורש, לא במרכז התצוגה של מפה. סניפים שנסגרו, עודפים, כפילויות ומיקומים חשודים נשמרים כסיבות השמטה לבדיקה.
4. מריצים בדיקה ללא שינוי המאגר, עם קבצים מפורשים של הסבב:

   ```sh
   npm run branches:review -- candidates/foxhome.json candidates/shilav.json candidates/new-chain.json
   ```

   הדוח `reports/branch-import-latest.json` מפרט תוספות, עדכונים, כפילויות, רשומות קודמות שנשמרו וקואורדינטות שדורשות בדיקה. מעבר של סניף קיים ביותר מ־150 מטר חוסם החלה עד בירור. יש לבדוק גם שמות, ערים וחריגי זכאות מול המקור; בדיקת תחום קואורדינטות לבדה אינה אימות מיקום.
5. לאחר בדיקת הדוח מחילים את אותם קבצים:

   ```sh
   npm run branches:apply -- candidates/foxhome.json candidates/shilav.json candidates/new-chain.json
   npm test
   VITE_BASE_PATH=/suvarim/ npm run build
   npm run test:browser
   ```

6. מבצעים commit ו־push, מפעילים את workflow הפרסום `pages.yml`, ממתינים להצלחה ובודקים באתר את ספירת המיקומים ואת הסינון לפי שובר. שינוי הנתונים המקומיים בלבד אינו מעדכן את האתר.

## שמירה ועדכון

לכל סניף מזהה יציב. איסוף חוזר מעדכן רשומות קיימות ואינו מוסיף כפילויות באותו מקום. רשומה חסרה בסבב חלקי נשמרת; סגירה או מחיקה דורשות בירור נפרד. גם רשת עם הרבה מיקומים נשארת בכיסוי חלקי כל עוד נותרו חריגים או שהמקור אינו מכסה את הרשת כולה.

מומלץ בסבבים הבאים לבדוק תחילה מקורות ששונו או שלא אומתו זמן רב, ולהחזיק אוסף נפרד לכל אתר. שינוי מבנה האתר צריך לעצור את האוסף לבדיקה, ולא למחוק את הקטלוג. דוח ההחלה ונתוני המקור נשמרים ב־Git כדי שאפשר יהיה להבין ולבטל שינוי נקודתי. קובצי המועמדים מכילים מידע ציבורי על סניפים בלבד; אין לשמור בהם שוברים אישיים או קודי מימוש.

סקריפטי `update-branches-*` הישנים כותבים ישירות למאגר: אין להריץ אותם במקביל. באיסוף חדש משתמשים בקובצי מועמדים ובשלב איחוד יחיד. אוטומציה מחזורית אפשר להוסיף בעתיד לאחר אישור נפרד, עם דוח שינויים ובדיקות לפני פרסום.
