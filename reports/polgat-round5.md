# פולגת — סבב חמישי, 20.09.2026

מקור רשמי: https://www.golfgroup.co.il/branches

נמצאו 21 רשומות פולגת דרך בחירת המותג `Polgat` במאתר הרשמי של קבוצת גולף. תגובת API הציבורית (`/_api/cloud-data/v2/items/query`) מציינת `count=21`, `offset=0`, `total=21`, `hasNext=false`. אלה כל הרשומות שהחזיר המסנן שנבדק, ולא טענה לגבי כל סניפי הרשת בארץ. נבחרו **13 מיקומים ייחודיים**, ו־8 נשארו בהשמטות. הכיסוי חלקי.

ה־HTML הראשוני הציג רק חלק מרשומות המותגים וכלל גם אוסף `Import1` ישן. הוא לא שימש כבסיס למועמדים. האוסף הסופי קורא את תגובת `Branches` המסוננת ל־Polgat שמתקבלת מפעולת הבחירה באתר. לא שימשו טוקני התחברות, פרופיל משתמש או נתונים פרטיים. נתוני הביקורת שומרים רק פרטי סניפים ציבוריים, מזהים, היקף התגובה והצלבות; לא נשמרו כותרות או אסימוני דפדפן.

לכל מיקום מקור ותאריך בדיקה. קואורדינטות `latitude`/`longitude` של הסניף הועתקו ללא שינוי, והוצלבו לפי שם המתחם או כתובתו עם מיקומים מרשת נוספת. האסמכתאות, הקישורים והמרחקים מופיעים ב־`scripts/data/polgat-round5-reviewed.json`. המזהים היציבים נגזרים מ־`code` הרשמי, ומזהי UUID המקוריים נשמרו בביקורת.

נפסלו נקודות ביג כרמיאל (יותר מ־700 מטר מהמתחם המאומת) וחוצות אשקלון (יותר מ־1.8 ק״מ מהאסמכתה), אאוטלט בני ברק לבירור זכאות, וכן חמישה מיקומים שהצלבתם לא הושלמה: מלכת שבא אילת, מגדל שלום, מבנה באר שבע, קניון ערים וחונים קונים. קרבה לטיילת אילת אינה מספיקה להוכחת כתובת מלכת שבא ולכן הרשומה נשארה לבירור.

`scripts/collect-polgat-candidates.mjs` משתמש בדפדפן נקי לבחירה הרשמית ושומר רק `candidates/polgat-round5.json`. הוא עוצר לפני כתיבה אם התגובה אינה מלאה, היקפה קטן מ־21, רשומה שנבדקה חסרה או השתנתה, המזהים כפולים או הנתונים אינם מתאימים למותג ולמיקום. רשומות חדשות דורשות בדיקה. המאגר המשותף לא נערך.

מקור הקבוצה עשוי לסייע בסבבים הבאים להשלמת חריגים ברשתות אחרות; בסבב זה נאספה פולגת בלבד.

בדיקה דרך פונקציית המיזוג הקיימת, ללא החלה: 13 תוספות, אפס כפילויות ואפס התראות תזוזה. אין זוג מועמדים במרחק קטן מ־300 מטר. האוסף הורץ מחדש בהצלחה מול המסנן הרשמי.
