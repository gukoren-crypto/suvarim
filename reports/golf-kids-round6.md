# GOLF KIDS — ביקורת סבב שישי, 20.09.2026

**לא נוספו מיקומים ולא שונו 13 המיקומים הקיימים.** נבדק המקור הרשמי החדש של הקבוצה: https://www.golfgroup.co.il/branches, באמצעות בחירת `Golf Kids & Baby`.

התקבלו כל 83 רשומות המסנן: עמוד ראשון 50 רשומות (`offset=0`, `total=83`, `hasNext=true`) ועמוד שני 33 (`offset=50`, `total=83`, `hasNext=false`). אלה רשומות המסנן שנבדק, לא קביעה בדבר מספר הסניפים הארצי. האוסף משתמש בדפדפן נקי ובדפדוף ציבורי; כותרות המשמשות לדפדוף נשארות בזיכרון בלבד ולא נשמרות בקבצים. קובץ הביקורת שומר מטא־נתוני עמודים ללא סמני דפדוף או אסימונים.

11 מיקומים שכבר מופו אותרו לפי הקוד והמתחם. שני מיקומים קיימים אינם ברשימה החדשה: `golf-kids-342` ביג פאשן אשדוד ו־`golf-kids-390` ביג גלילות. שניהם נשמרו ללא שינוי; היעדרות מרשימה אינה ראיית סגירה. גם מקור, תאריך, כתובת, קואורדינטות ומזהים בכל 13 הרשומות נשמרו ללא שינוי.

72 הרשומות הנוספות נשארו לבדיקה. המקור החדש עדיין כולל קואורדינטות בסדר הפוך, עודפים, נקודות של מתחם אחר ומתחמים שהצלבתם לא הושלמה. לדוגמה, יכין סנטר מצביע לקניון הגדול בפתח תקווה וקניון הנגב מצביע לביג באר שבע. לא הוחלפו צירים ולא הועתקו נקודות מרשת אחרת. קרבה בעיר לבדה לא נחשבה לאימות. מיפוי בין רשומות נעשה לפי קוד ומתחם, ללא יצירת מזהה חדש לנקודה קיימת.

`scripts/data/golf-kids-round6-reviewed.json` מכיל את 83 רשומות המקור המצומצמות, סיבות ההשמטה ואת צילום 13 הרשומות ומרשם המקורות שנשמרו. האוסף `scripts/collect-golf-kids-group-candidates.mjs` מאמת מחדש את כל העמודים ועוצר לפני כתיבה אם חסרות רשומות, יש כפילויות או שדות השתנו.

`candidates/golf-kids-round6.json` הוא קובץ ללא שינוי נתונים לצורך בדיקת תהליך המועמדים: אותם 13 מיקומים ואותו מרשם מקור בדיוק. ספירת 119 במרשם זה מתייחסת למקור ההיסטורי מסבב 3, ולא ל־83 הרשומות שנבדקו כאן. אין החלפת מקור119 בספירה מטעה83, ואין ערבוב בין היקפי המקורות. תוצאת הסבב לרשת היא דוח בירור בלבד.

האוסף שוחזר בהצלחה מול שני העמודים. בדיקת פונקציית המיזוג אישרה אפס תוספות, אפס עדכונים, אפס כפילויות ואפס התראות תזוזה; המאגר ומרשם המקורות לאחר המיזוג זהים בדיוק למקור. לא נערכו קבצים משותפים או נתוני שוברים פרטיים.
